# Rocket.Chat 适配器开发/测试依赖
#
# 这些依赖在 AstrBot 运行时由本体提供（见 requirements.txt 说明），
# 此处声明仅用于脱离 AstrBot 单独运行 tests/ 下的单元测试。
#
# 版本下限不低于 AstrBot 本体（aiohttp>=3.11.18、cryptography>=44.0.3），
# 并由 Dependabot 持续跟进上游，使测试可在较新版本上验证前向兼容性。
# 注意：测试版本可能领先于本体实际运行版本，回归请以本体版本为准。

aiohttp>=3.14.3
cryptography>=49.0.0

# 代码质量工具（lint / format / 类型检查）
# 类似 cargo clippy 的体验
ruff>=0.15.20
pyright>=1.1.411
